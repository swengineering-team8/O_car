<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <title><%= title %></title>
    <script src="/javascripts/jquery-3.6.0.min.js"></script>
    <link rel="canonical" href="https://getbootstrap.com/docs/5.2/examples/carousel/">
    <link href="/stylesheets/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="/stylesheets/common.css" rel="stylesheet">
    <link href="/stylesheets/carousel.css" rel="stylesheet">
  </head>
  <body>
    <%-include('navbar_loggedin.ejs') %>
    <main>
      <div class="container">
        <div class="py-5 text-center">
          <img class="d-block mx-auto mb-4" src="/logo.svg" alt="" width="70" height="70">
          <h2>문의하기</h2>
          <p class="lead">불편하신 사항들을 아래에 기재해주세요!</p>
          <% if (messages.error) { %>
            <p style="color:red"><%- messages.error %></p>
            <% } %>
            <% if (messages.success) { %>
            <p style="color:green"><%- messages.success %></p>
          <% } %>
        </div>
        <div class="container col-md-8">
          <form action="/auth/contact-post" method="post" onsubmit="return onWriteSubmit()">
            <div class="mb-3">
              <label for="qst_title" class="form-label">문의 제목</label>
              <input type="text" name="qst_title" class="form-control" id="qst_title" placeholder="제목을 작성하세요">
            </div>
            <div class="mb-3">
              <label for="qst_content" class="form-label">문의 내용</label>
              <textarea type="text" name="qst_content" class="form-control" rows="5" id="qst_content" placeholder="내용을 작성하세요"></textarea>
            </div>
            <input class="w-100 btn btn-lg btn-primary mb-3" type="submit" value="제출하기"></input>
          </form>
        </div>
      </div>
      <%-include('footer.ejs') %>
    </main>
    <script src="/stylesheets/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script>
      function onWriteSubmit() {
        if($("#qst_title").val().trim()=="")
        {
          $("#qst_title").val("");
          $("#qst_title").focus();
          alert("비우면 안 됩니다!!");
          return false;
        }
        if($("#qst_content").val().trim()=="")
        {
          $("#qst_content").val("");
          $("#qst_content").focus();
          alert("비우면 안 됩니다!!");
          return false;
        }
        
      }
    </script>
  </body>
</html>
